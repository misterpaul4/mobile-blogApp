import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  header: {
    fontSize: 20,
    textAlign: "center",
    fontWeight: "bold",
    marginVertical: 20,
  },
});

export default globalStyles;
